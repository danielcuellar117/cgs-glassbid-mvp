[pytest]
testpaths = tests/worker tests/infra
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests as integration (requires Docker services)
    slow: marks tests as slow running
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
